---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-http
spec:
  selector:
    matchLabels:
      app: nginx
  replicas: 1
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: cgr.dev/chainguard/nginx:1.27.4
        ports:
        - name: http
          containerPort: 8080
        livenessProbe:
          httpGet:
            path: /
            port: http
        readinessProbe:
          httpGet:
            path: /
            port: http
        securityContext:
          rnuAsNonRoot: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
        - name: mtls-buc-lenovo
          mountPath: /usr/share/nginx/html/buc-lenovo
      volumes:
      - name: mtls-buc-lenovo
        secret:
          secretName: mtls-buc-lenovo

---
kind: Service
apiVersion: v1
metadata:
  name: cert-httpconsole
spec:
  selector:
    app: nginx
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cert-http
spec:
  rules:
  - host: cert-http.10.0.0.16.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cert-http
            port:
              name: http