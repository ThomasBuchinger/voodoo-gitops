apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: pihole
  namespace: kube-system
spec:
  repo: https://mojo26000.github.io/pihole-kubernetes
  chart: pihole
  targetNamespace: pihole
  valuesContent: |-
    image:
      tag: "2022.07"
    dnsHostPort:
      enabled: false
    serviceDns:
      mixedService: true
      type: NodePort
      nodePort: 53
    serviceDhcp:
      enabled: false
    # serviceWeb: {}
    virtualHost: pihole.buc.sh
    ingress:
      enabled: true
      hosts:
      - pihole.buc.sh
      - pihole.10.0.0.16.nip.io
    admin:
      existingSecret: admin
      passwordKey: password
    dnsmasq:
      customEntries:
      - address=/nas.buc.sh/10.0.0.19
      - address=/kvm.buc.sh/10.0.0.13
      - address=/voodoo.buc.sh/10.0.0.16
      customCnameEntries:
      - cname=pihole.buc.sh,voodoo.buc.sh 
